const http = require('http');
const url = require('url');
const express = require('express');
const { Client } = require('@google-cloud/bigquery');
const app = express();

app.get('fetch_latest*', async (req, res) => {
  
});
