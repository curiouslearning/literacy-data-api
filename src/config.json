{
  "port":"3000",
  "fetchLatestQuery": {
    "string": "SELECT * FROM @table WHERE _TABLE_SUFFIX BETWEEN FORMAT_DATE('%Y%m%d', DATE_SUB(CURRENT_DATE(), INTERVAL 7 DAY)) AND FORMAT_DATE('%Y%m%d', DATE_SUB(CURRENT_DATE(), INTERVAL 1 DAY)) AND event_timestamp + event_timestamp_offset >= @cursor",
    "loc": "US"
  }
}
